# BGG XML API2 — authenticated access token.
# Get yours from BoardGameGeek and copy this file to .env.local
# (which is gitignored). On Netlify, set the same name in Site
# Settings > Environment variables.
BGG_API_TOKEN=
